% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pk_coef.R
\name{pk_coef}
\alias{pk_coef}
\title{Extract posterior draws or summaries for a specific term}
\usage{
pk_coef(
  fit,
  parameter,
  term,
  summarize = TRUE,
  source = "posterior",
  conf.level = 0.95
)
}
\arguments{
\item{fit}{A fitted \code{pk_fit} object, as returned by \code{pk_fit()}.}

\item{parameter}{One of \code{"c1"}, \code{"c2"}, \code{"mu1"}, \code{"mu2"}, \code{"sigma1"},
\code{"sigma2"} — which schedule parameter's formula the term belongs to.}

\item{term}{The term label to extract, as it appears in that parameter's
formula (e.g. \code{"(Intercept)"}, \code{"race"}, \code{"s(province)"}).}

\item{summarize}{If \code{TRUE} (default), return a tibble of posterior
summaries; if \code{FALSE}, return the \code{n_draws} x \code{n_coef} matrix of draws.}

\item{source}{\code{"posterior"} (default) or \code{"prior"}; \code{"prior"} requires the
fit to have been created with \code{sample_prior = "yes"} or \code{"only"}.}

\item{conf.level}{Credible interval width used when \code{summarize = TRUE}.}
}
\value{
If \code{summarize}, a tibble with one row per level/column of \code{term}
and columns \code{level}, \code{mean}, \code{median}, \code{lower}, \code{upper}. If not, an
\code{n_draws} x \code{n_levels} matrix of draws, with columns named by level.
}
\description{
Extracts the working-scale regression coefficients for a single term of a
single schedule parameter's formula (e.g. the \code{race} term of \code{c1}'s
formula), either as raw draws or summarized with a point estimate and
credible interval. For a term with multiple levels or basis columns (a
factor, or a smooth/random-effect term), one column is returned per level.
}
\examples{
\dontrun{
pk_coef(fit, parameter = "c1", term = "race")
pk_coef(fit, parameter = "c1", term = "(Intercept)", summarize = FALSE)
}

}
