% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pk_predict.R
\name{predict.pk_fit}
\alias{predict.pk_fit}
\title{Predict fertility schedule quantities from a fitted PK model}
\usage{
\method{predict}{pk_fit}(
  object,
  newdata = NULL,
  type = c("asfr", "schedule_params", "linear_predictor", "schedule_curve"),
  source = c("posterior", "prior"),
  age = NULL,
  age_grid = NULL,
  summarize = TRUE,
  conf.level = 0.95,
  ndraws = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted \code{pk_fit} object, as returned by \code{pk_fit()}.}

\item{newdata}{A data frame giving the covariate values to predict at
(and, for \code{type = "asfr"}, ages as well, unless \code{age} is supplied
separately). Defaults to the training data (\code{object$data}) if \code{NULL}.}

\item{type}{One of \code{"asfr"}, \code{"schedule_params"}, \code{"linear_predictor"}, or
\code{"schedule_curve"} (see Description).}

\item{source}{Whether to predict from \code{"posterior"} (default) or \code{"prior"}
draws; \code{"prior"} requires the fit to have been created with
\code{sample_prior = "yes"} or \code{"only"}.}

\item{age}{Only used when \code{type = "asfr"}. One of \code{NULL} (use the age
column named in \code{object$age_col}, looked up in \code{newdata}), a string
naming a column of \code{newdata} to use as age, or a numeric vector of ages
with length \code{nrow(newdata)}.}

\item{age_grid}{Only used when \code{type = "schedule_curve"}. A numeric vector
of ages at which to evaluate the schedule; defaults to \code{seq(15, 49, by = 1)}.}

\item{summarize}{If \code{TRUE} (default), collapse draws into a point estimate
(mean and median) and a \code{conf.level} credible interval. If \code{FALSE},
return the raw draws.}

\item{conf.level}{Credible interval width used when \code{summarize = TRUE}.}

\item{ndraws}{Optional number of draws to randomly subsample before
summarizing or returning; \code{NULL} (default) uses all available draws.}

\item{...}{Unused; present for S3 method consistency.}
}
\value{
The shape depends on \code{type} and \code{summarize}:
\describe{
\item{\code{type = "asfr"}}{If \code{summarize}, a tibble with one row per row of
\code{newdata} and columns \code{.fitted}, \code{.median}, \code{.lower}, \code{.upper}. If
not, an \code{n_draws} x \code{nrow(newdata)} matrix of ASFR draws.}
\item{\code{type = "linear_predictor"} or \code{"schedule_params"}}{If
\code{summarize}, a tibble with one row per row of \code{newdata} and, for each
of the six schedule parameters, columns named
\verb{<parameter>.fitted}/\code{.median}/\code{.lower}/\code{.upper}. If not, a named
list (one element per schedule parameter) of \code{n_draws} x
\code{nrow(newdata)} matrices.}
\item{\code{type = "schedule_curve"}}{If \code{summarize}, a long-format tibble
with one row per (cell, age) combination, including the covariate
columns from \code{newdata}, \code{age}, and \code{.fitted}/\code{.median}/\code{.lower}/
\code{.upper}. If not, an \code{n_draws} x \code{n_cells} x \code{n_ages} array (with
\code{age_grid} values as the third dimension's names).}
}
}
\description{
Computes model-implied quantities from a fitted \code{pk_fit} object at a
set of covariate values, using posterior (or prior) draws. Four kinds of
quantity can be requested via \code{type}:
\itemize{
\item \code{"asfr"}: the age-specific fertility rate at specific (row, age)
combinations in \code{newdata}.
\item \code{"linear_predictor"}: the working-scale linear predictor \code{eta} for
each of the six schedule parameters (\code{c1}, \code{c2}, \code{mu1}, \code{mu2},
\code{sigma1}, \code{sigma2}), one value per row of \code{newdata}.
\item \code{"schedule_params"}: the same six schedule parameters transformed
back to their natural scale (see \code{pk_effects()} for the
working-scale transforms), one value per row of \code{newdata}.
\item \code{"schedule_curve"}: the full fertility schedule evaluated over
\code{age_grid}, for each distinct covariate combination ("cell") implied
by \code{newdata}.
}
}
\examples{
\dontrun{
predict(fit, type = "asfr")
predict(fit, newdata = pred_levels, type = "schedule_params")
predict(fit, type = "schedule_curve", conf.level = 0.95)
}

}
